# C-Emulator-for-MIPS-Assembly-Code
A C emulator that loads a raw MIPS binary, executes it through a modeled 5-stage pipeline with hazard detection and forwarding, runs it against a simulated cache hierarchy, and exposes memory-mapped peripherals
